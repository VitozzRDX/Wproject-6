/**
 * unit-defs.js — статические определения всех типов и подтипов юнитов.
 *
 * Ключ объекта — идентификатор типа: {нация}_{категория}_{атака-защита-движение}_{класс}
 * Пример: ge_inf_467_1 = немецкая пехота 4-6-7, класс "1st Line"
 *
 * Поля:
 *   nation   — нация ('german' | 'soviet' | ...)
 *   category — тип юнита ('infantry' | 'vehicle' | 'artillery' | 'gun')
 *   class    — боевой класс:  E=Elite, 1=1st Line, 2=2nd Line, G=Green, C=Conscript
 *   mf       — movement factor: очки движения за ход (G и C = 3, остальные = 4)
 *   image    — путь к графике
 *
 * Новые типы добавляются сюда — логика читает поля по имени.
 */
export const unitDefs = {

  // ── Немецкая пехота ────────────────────────────────────────────────────────

  /** Немецкое пехотное отделение 4-6-7, класс 1st Line */
  ge_inf_467_1: {
    nation:   'german',
    category: 'infantry',
    class:    '1',
    mf:       4,
    image:    'grafic/ge467S.gif',
  },

};
